version: 1
update_configs:
  - package_manager: "javascript"
    directory: "/"
    update_schedule: "live"
    allowed_updates:
      - match:
          dependency_name: "@textile/*"
          dependency_type: "all"
    automerged_updates:
      - match:
          dependency_type: "all"
          update_type: "all"
